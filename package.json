{
  "name": "rocket",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --bun --watch run ./index.ts",
    "build": "bun build ./index.ts --compile --outfile ~/dev/executables/rocket"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0"
  }
}